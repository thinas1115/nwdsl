# nwdsl リポジトリ固有ルール

## コミットメッセージにCo-Authored-Byを付けない
このリポジトリはGitHub公開リポジトリ。コミットに `Co-Authored-By: Claude ...` トレーラーを
付けると、GitHubのContributorsページに "claude" が別アカウントとして表示されてしまう。

**このリポジトリでは、コミットメッセージに `Co-Authored-By: Claude` 系のトレーラーを一切付けないこと。**
著者・コミッターは常にユーザー自身のgit設定 (thinas1115) のままにする。
